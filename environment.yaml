name: interlocking-backprop
channels:
    - pytorch
    - defaults
dependencies:
    - python=3.9.*
    - pytorch=1.10
    - torchvision
    - pytest
    - black
    - mypy
    - pip
    - pip:
        - -e .
